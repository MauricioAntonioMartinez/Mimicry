import React from "react";
import { StyleSheet, Text, View } from "react-native";

interface Props {}

export const ClipBoardItem = (props: Props) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

const styles = StyleSheet.create({});
